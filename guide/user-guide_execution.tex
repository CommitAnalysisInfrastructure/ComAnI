\subsection{Execution}
\label{sec:Execution}
The execution of \thetool{} requires the following preparation steps:
\begin{enumerate}
	\item Clone or checkout the software repository you want to analyze to your favorite directory
	\item Create a \thetool{} configuration file, e.g. \fselement{my-instance.properties}
\end{enumerate}
For the second step, the infrastructure offers the parameters described in Listing~\ref{lst:CoreConfigurationParameters} to Listing~\ref{lst:AnalysisConfigurationParameters}.
\begin{figure}[ht]
	\centering
		\lstinputlisting[caption={\thetool{} core configuration parameters},label=lst:CoreConfigurationParameters,basicstyle=\small,language=Properties]{inserts/configuration-parameters_core.txt}
\end{figure}
\begin{figure}[ht]
	\centering
		\lstinputlisting[caption={\thetool{} extraction configuration parameters},label=lst:ExtractionConfigurationParameters,basicstyle=\small,language=Properties]{inserts/configuration-parameters_extraction.txt}
\end{figure}
\begin{figure}[ht]
	\centering
		\lstinputlisting[caption={\thetool{} analysis configuration parameters},label=lst:AnalysisConfigurationParameters,basicstyle=\small,language=Properties]{inserts/configuration-parameters_analysis.txt}
\end{figure}

The actual execution takes place via the following steps:
\begin{enumerate}
	\item Open a Terminal (Ubuntu) or Command Prompt (Windows)
	\item Navigate to the \thetool{} directory, e.g., the one used in step 5 in Section~\ref{sec:Download}
	\item Start the infrastructure with the desired configuration file: \clcommand{java –jar \thetool{} my-instance.properties}
\end{enumerate}

Please note that there may be plug-in-specific configuration parameters, which are not covered by the following listings.
We recommend reading the respective Github pages to find out more about such parameters and their usage.